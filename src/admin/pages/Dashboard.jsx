import React from 'react';
/* No need for layout components here as they are in AdminLayout */

const Dashboard = () => {
    return (
        <div className="dashboard-view">
            <h1 className="admin-page-title">Dashboard Overview</h1>
        </div>
    );
};

export default Dashboard;
